let player = Ecs.component (module Player0)

let client = Ecs.component (module Client0)

let room = Ecs.component (module Room0)

let item = Ecs.component (module Item0)

let targetable = Ecs.component (module Targetable)

let location = Ecs.component (module Location)
